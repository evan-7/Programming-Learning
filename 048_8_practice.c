#include <stdio.h>
#include <stdlib.h>

int main() {
	/*
	char ch;
	while ((ch = getchar()) != '\n') {
		putchar(ch++);
		putchar(++ch);

		continue;
	}
	*/

	/*
	int count = 0;
	FILE *fp;
	char file_name[50];
	char ch;

	printf("Filename: ");
	scanf("%s", file_name);

	fp = fopen(file_name, "r");

	
	if (fp == NULL) {
		printf("Invalid file.\n");
	}

	while ((ch = getc(fp)) != EOF) {
		putchar(ch);
		count++;
	}
	

	printf("%s has %d characters in total.\n", file_name, count);
	*/




	





	return 0;
}